---
id: reload
title: /undeadpandemic reload
sidebar_label: Reload
description: Reloads the UndeadPandemic configuration file (config.yml).
---

# `/undeadpandemic reload`

The `/undeadpandemic reload` command reloads the plugin’s primary configuration file (`config.yml`) without needing to restart your server. This is useful for quickly applying configuration changes during development or server setup.

Both official syntaxes function identically: